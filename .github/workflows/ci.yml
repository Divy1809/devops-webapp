name: CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Print Message
        run: echo "Build Successful!"

      - name: List Files
        run: ls -la

      - name: Send Slack Notification
        run: |
          curl -X POST -H "Content-type: application/json" \
          --data '{"text":"✅ GitHub Action Build Completed Successfully 🚀"}' \
          ${{ secrets.SLACK_WEBHOOK }}